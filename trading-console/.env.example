# Trading Console frontend proxy target.
# The frontend serves only UI assets and forwards GET /api/trading-console/*
# to this backend read-model server.
TRADING_CONSOLE_API_BASE="http://127.0.0.1:8000"

# Optional UI server port.
PORT="3000"

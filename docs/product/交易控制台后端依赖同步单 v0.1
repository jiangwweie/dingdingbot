# 交易控制台后端依赖同步单 v0.1

## 0. 定位

本同步单来自“交易控制台”前端重构需求澄清。

它不是 Codex 任务，不要求立即实现。
用途是把前端重构反推出的后端依赖同步给主控窗口，由主控统一判断是否纳入后端主线、如何排期、是否与当前执行链路 / 版本治理 / 实盘安全边界冲突。

本单只记录：

- 页面需要什么事实
- 当前代码支持什么
- 缺口是什么
- 属于只读 read model、安全 action，还是架构治理
- 是否触达交易
- 是否 PG mutation
- 需要主控决策什么

------

## 1. 首页 / 真实风险总览

### 页面需要什么事实

- 当前环境：LIVE / TESTNET / DEV / UNKNOWN
- 当前 profile / account scope
- GKS 状态
- startup guard 状态
- 账户权益 / 钱包余额 / 可用保证金
- 全账户真实仓位
- 全账户真实挂单，包括普通挂单与条件 / algo 单
- PG 当前 open orders / open intents
- PG 与交易所是否一致
- 是否存在未知订单 / 外部订单 / 未归属订单
- 当前是否有有效有界实盘授权
- 当前是否有未完成实盘行动
- 当前唯一待处理事项
- 数据新鲜度：last_updated_at / exchange_snapshot_at / freshness_status

### 当前代码支持什么

- 已有账户相关事实能力：equity、wallet balance、margin、unrealized PnL、exchange positions、exchange open orders、PG orders、reconciliation、GKS、startup guard。
- 交易所订单普通挂单和条件 / algo 单在 gateway 层有 fetch 能力。
- PG orders、positions 有表与 API 基础。
- reconciliation 有服务与部分 API。

### 缺口是什么

- 缺少面向首页的统一 `dashboard_state` read model。
- 缺少稳定的数据新鲜度字段：
  - `last_updated_at`
  - `exchange_snapshot_at`
  - `freshness_status`
  - `exchange_error`
- 当前前端若自行组合多个 API，容易误把 stale / unknown 数据展示为安全。
- 需要明确 UNKNOWN / STALE 时的后端状态语义，不应由前端自行推断。

### 类型

只读 read model。

### 是否触达交易

否。只读交易所查询可能存在，但不应触发订单 / 撤单 / 平仓。

### 是否 PG mutation

否。

### 需要主控决策什么

- 是否建设 `/api/trading-console/dashboard` 或等价聚合 read model。
- stale / warning / fresh 的后端判定阈值由后端统一定义，还是由配置项控制。
- 首页是否只允许调用 trading-console namespace 下的接口。

------

## 2. 账户总览

### 页面需要什么事实

- 全账户仓位，不限当前 Carrier / 当前 symbol。
- 每个仓位：
  - symbol
  - side / positionSide
  - quantity
  - entry price
  - mark price
  - unrealized PnL
  - realized PnL
  - leverage
  - margin mode
  - 是否系统归属
  - 是否有保护单
  - 是否存在异常风险
- 全账户挂单摘要。
- 账户风险状态。
- 数据新鲜度和交易所 degraded / unknown 状态。

### 当前代码支持什么

- `positions` 表存在。
- `PGPositionORM` 有 `realized_pnl`、`unrealized_pnl`、`entry_price`、`mark_price`、`leverage` 等字段。
- `GET /api/runtime/positions` 和 `GET /api/v3/positions` 存在，其中 v3 语义更完整。
- account snapshot 可优先使用交易所实时 positions。

### 缺口是什么

- ConsolePositionItem 缺 `realized_pnl`。
- 缺少账户总览专用 read model，无法直接表达“全账户风险状态”。
- 仓位是否系统归属、是否被当前实盘行动覆盖、是否有保护单，当前没有前端友好的聚合字段。
- 缺少统一 freshness 语义。

### 类型

只读 read model。

### 是否触达交易

否。可包含只读交易所查询。

### 是否 PG mutation

否。

### 需要主控决策什么

- 是否补 `/api/trading-console/account-risk` 或等价接口。
- 是否把 `realized_pnl` 暴露到交易控制台账户总览。
- 仓位归属与保护状态由哪个后端聚合层输出。

------

## 3. 订单台账

### 页面需要什么事实

订单台账是一级页面。需要区分并聚合：

- 交易所订单
- 本地 PG 订单
- 成交 / fills
- 当前仓位
- Entry 建仓单
- TP 保护单
- SL / algo 保护单
- Recovery close 订单
- 外部 / 手动订单
- 未知归属订单
- PG-only 订单
- Exchange-only 订单
- 状态不一致订单
- 孤儿保护单
- 保护单组与建仓单的关系
- 订单对应的实盘行动、授权、ExecutionIntent、Review

### 当前代码支持什么

- `orders` 表存在。
- PGOrder 字段包含：
  - `id`
  - `signal_id`
  - `symbol`
  - `direction`
  - `order_type`
  - `order_role`
  - `status`
  - `price`
  - `trigger_price`
  - `requested_qty`
  - `filled_qty`
  - `average_exec_price`
  - `reduce_only`
  - `exchange_order_id`
  - `parent_order_id`
  - `oco_group_id`
  - `exit_reason`
  - `filled_at`
  - `created_at`
  - `updated_at`
- Repository 支持按 symbol / status / role / limit / offset 查询。
- API 已有：
  - `/api/v3/orders`
  - `/api/v3/orders/tree`
  - `/api/runtime/execution/orders`
- TP/SL 可通过 `parent_order_id` 和 `oco_group_id` 与 Entry 形成保护单组。
- exchange gateway 层支持普通挂单、条件 / algo 单、单笔订单查询。
- reconciliation 能发现部分 unknown / unmanaged orders。

### 缺口是什么

- 无交易控制台专用 `order_ledger` read model。
- 交易所订单数据未持久化，仅 gateway transient。
- 无独立 exchange orders API。
- 条件 / algo 订单需要专门 fetch，但前端没有统一接口。
- OrderAuditLog 表存在但无 REST API。
- 无统一字段表达：
  - exchange-only
  - PG-only
  - mismatch
  - external/manual
  - unknown attribution
  - orphan protection
- `ConsoleOrderItem` 没有完整暴露 `filled_qty` / `average_exec_price`。
- client_order_id 当前不持久化，但是否必须补为 P0 需要主控判断；前端 v1 可优先依赖 `exchange_order_id`、`order_id`、`execution_intent_id`、`authorization_id`、`parent_order_id`、`oco_group_id` 做归属。

### 类型

只读 read model 为主。
可能涉及后续架构治理。

### 是否触达交易

只读查询可能触达交易所。
不应触发下单 / 撤单 / 平仓。

### 是否 PG mutation

订单台账 read model 不应 mutation。
若后续要持久化 exchange snapshot / fills，则属于架构治理，涉及 PG mutation。

### 需要主控决策什么

- 是否建设 `/api/trading-console/order-ledger` 聚合 read model。
- 订单归属规则是否先基于现有字段，不强制引入 `client_order_id`。
- 是否把 `client_order_id` 作为 P1/P2 异常恢复增强，而不是 P0。
- 是否需要交易所订单快照持久化。
- 是否需要单独的 order audit API。

------

## 4. 保护单组 / Protection Set

### 页面需要什么事实

- Entry 建仓单
- TP 保护单
- SL / algo 保护单
- 保护价格计划
- 保护单是否真实挂到交易所
- TP/SL 是否都存在
- 是否部分保护失败
- 是否出现孤儿保护单
- 是否出现交易所有仓位但本地无保护
- 是否出现本地保护单交易所不可见
- 当前保护状态：
  - protected
  - partially_protected
  - unprotected
  - unknown
  - orphaned

### 当前代码支持什么

- TP/SL 通过 `parent_order_id` 挂在 Entry 订单下。
- `oco_group_id` 可表达保护单组。
- `brc_protection_price_plans` 存在，记录 TP/SL 价格、数量、tick size、amount step、rounding。
- `protection_health_monitor.py` 内部可检测：
  - local SL missing on exchange
  - orphan reduce-only order
  - exchange position untracked

### 缺口是什么

- protection health monitor 无 API 暴露。
- 保护健康状态没有交易控制台 read model。
- 前端不能直接知道 protected / partial / unprotected。
- retry protection / cancel protection 当前无前端安全 action API。
- 不建议前端自行从 orders 里推断保护健康。

### 类型

只读 read model。
后续可能派生安全 action。

### 是否触达交易

read model 可包含只读交易所查询。
retry / cancel 等动作若后续开放，会触达交易。

### 是否 PG mutation

read model 不 mutation。
retry / recovery task 更新会涉及 PG mutation。

### 需要主控决策什么

- 是否建设 `/api/trading-console/protection-health`。
- protected / partially_protected / unprotected 状态由哪个服务统一判定。
- 是否允许前端看到保护健康内部检测结果。
- retry protection 是否进入后续安全 action API。

------

## 5. 异常恢复 / Recovery & Exception

### 页面需要什么事实

- 当前 blocker 是什么
- 阻断的是授权、执行，还是新实盘行动
- 是否存在未保护仓位
- 是否存在残留挂单
- 是否存在 unknown / unmanaged order
- 是否存在 PG/exchange mismatch
- 是否存在 recovery task
- recovery task 状态：
  - pending
  - retrying
  - resolved
  - failed
- 可执行动作
- 不可执行动作及原因
- 哪些动作需要技术处理
- 哪些动作会触达交易所
- 哪些动作只是 PG 状态动作

### 当前代码支持什么

- `execution_recovery_tasks` 表存在。
- Recovery repository 存在。
- recovery retry policy 存在。
- startup reconciliation、periodic reconciliation、protection health monitor、external close monitor 都存在内部逻辑。
- `POST /api/reconciliation` 可手动触发对账。
- health 概览暴露部分 recovery 计数。
- `/api/runtime/events` 包含 recovery 事件。

### 缺口是什么

- 无 recovery task list API。
- 无手动 retry recovery task API。
- 无 recovery resolve / mark manual review API。
- 无 protection health API。
- 无 reconciliation history 查询 API。
- 无 ghost / orphan orders 查询 API。
- Recovery 页面目前只能部分按钮化：
  - 手动对账可按钮化
  - 其他动作多数只能显示“需要后端支持 / 技术处理”
- cancel / flatten / retry protection 等真实交易动作没有交易控制台安全 action API。

### 类型

只读 read model + 安全 action。

### 是否触达交易

read model 不触达交易，只读查询除外。
未来 cancel order / flatten / retry protection 会触达交易，必须单独主控审批。

### 是否 PG mutation

查看 recovery 不 mutation。
retry / resolve / mark manual review / void authorization 会 PG mutation。
cancel / flatten 同时触达交易并可能 PG mutation。

### 需要主控决策什么

- Recovery v1 是否先做 read-only + 对账按钮。
- 是否允许新增 recovery task list API。
- 哪些恢复动作可以进入交易控制台。
- 真实交易动作是否必须另起安全 action Sprint。
- 是否需要 Owner 二次确认和 idempotency guard。

------

## 6. 有界实盘授权

### 页面需要什么事实

- 风险确认状态
- 授权草案状态
- 有界实盘授权状态
- 授权是否 active
- 授权是否 consumed
- 授权是否 expired
- 授权是否 cancelled / voided
- 授权是否 actionable
- 授权 scope：
  - carrier
  - strategy family
  - symbol
  - side
  - cap
  - max notional
  - profile
  - environment
- 是否存在重复未消费授权
- 是否因仓位 / 挂单 / unresolved exposure 被阻断
- 可执行下一步动作

### 当前代码支持什么

- risk acknowledgement、authorization draft、bounded live authorization 主链路存在。
- authorization 可关联 carrier、strategy family、draft。
- execution intent 可关联 authorization。
- consumed authorization 存在。
- active authorization lookup 存在。
- scope 中 carrier / symbol / side / cap / max notional 基础支持存在。

### 缺口是什么

- 缺少显式前端字段：
  - `is_actionable`
  - `is_consumed`
  - `is_expired`
  - `is_cancelled`
  - `scope_match`
  - `blocking_reason`
- authorization TTL / expiry 需要确认或补齐。
- cancel / void authorization API 需要确认或补齐。
- environment / profile 是否作为独立 scope 持久化需要主控确认。
- 前端不应从 flow step 自行推断授权是否可执行。

### 类型

只读 read model + 安全 action。

### 是否触达交易

授权查看、作废授权不触达交易。
授权本身不等于执行，不应下单。

### 是否 PG mutation

查看不 mutation。
创建 / void / cancel authorization 会 PG mutation。

### 需要主控决策什么

- 是否建设 `/api/trading-console/authorization-state`。
- 是否增加 void authorization。
- authorization TTL 是否 P0。
- `is_actionable` 是否由后端统一计算。
- active authorization 与 unresolved exposure 冲突时的后端规则。

------

## 7. 实盘执行控制

### 页面需要什么事实

- 当前是否存在有效授权
- 授权是否 actionable
- final hard gate 总状态
- 每个 gate 的 pass / warning / block / unknown
- 每个 blocker 的 human message、technical code、required action
- 执行计划预览
- entry order 计划
- protection plan preview
- 是否会提交真实订单
- 执行后状态：
  - intent created
  - order submitted
  - order filled
  - protection placed
  - partial protection
  - failed
  - recovery required
- 重复执行防护
- consumed authorization 防护

### 当前代码支持什么

- final hard gate dry-run 存在。
- execution plan preview 存在。
- execution intent creation 存在。
- live execution trigger 存在。
- entry order status 存在。
- fill-based TP/SL 保护链路存在。
- review packet 存在。

### 缺口是什么

- 缺少面向前端的 execution-control 聚合状态。
- final gate 需要 per-gate 结构化返回。
- 执行按钮当前语义风险较高，需要新 API contract 和 UI 安全规则。
- partial protection failure 当前更多是内部检测，前端不可见。
- recovery action 不完整。
- 执行进度状态需要后端统一暴露，不应前端拼。

### 类型

只读 read model + 高风险安全 action。

### 是否触达交易

read model 不触达交易，只读查询除外。
execute action 会触达真实交易，属于最高风险动作。

### 是否 PG mutation

read model 不 mutation。
execute 会创建 / 更新 ExecutionIntent、Order、Review 等 PG 状态，并触发交易所订单。

### 需要主控决策什么

- 是否建设 `/api/trading-console/execution-control`。
- 旧 `/owner-trial-flow/.../execute` 是否迁移或包一层交易控制台 API。
- final gate per-gate 格式是否 P0。
- 执行按钮二次确认、安全文案、idempotency 由主控定义。
- 是否禁止旧前端执行按钮继续暴露。

------

## 8. 实盘复盘

### 页面需要什么事实

复盘同时看交易结果和系统链路：

交易结果：

- entry price
- exit price
- filled quantity
- average entry / exit price
- realized PnL
- fee
- fee asset
- funding
- slippage
- TP/SL 是否触发
- final position
- final open orders

系统链路：

- authorization
- final gate
- execution intent
- entry order
- TP order
- SL / algo order
- recovery task
- protection result
- failure reason
- root cause
- manual intervention
- issue governed / fixed
- audit chain

### 当前代码支持什么

- PGOrder 有 `average_exec_price`、`filled_qty`、`filled_at`。
- PGPosition 有 `realized_pnl`、`unrealized_pnl`。
- BRC campaign 有 campaign-level realized_pnl。
- Review packet / evidence 存在。
- orders、positions、execution_intents、authorization DB 链路可串起来。
- Daily risk stats 有聚合 PnL 相关数据。

### 缺口是什么

- 无逐笔 fills 表。
- fee 不在 PGOrder。
- fee asset 缺失。
- funding 实盘未追踪。
- slippage 实盘未追踪。
- ConsolePositionItem 缺 realized_pnl。
- Review 页面不应伪造 fee / funding / slippage。
- 缺少 review 专用 read model。

### 类型

只读 read model + 架构治理。

### 是否触达交易

否。

### 是否 PG mutation

复盘读取不 mutation。
若补 fills / fees / funding 持久化，则涉及 PG mutation 和可能的 migration。

### 需要主控决策什么

- 实盘复盘 v1 是否接受 fee / funding / slippage 显示为 not_available。
- 是否优先暴露已有 `realized_pnl` / `average_exec_price` / `filled_qty`。
- 是否建设 fills 表或先做轻量成交汇总。
- PnL 计算由后端统一做还是只展示已有数据。

------

## 9. 技术审计

### 页面需要什么事实

- 按 authorization_id 查完整链路
- 按 execution_intent_id 查完整链路
- 按 order_id 查订单生命周期
- 按 exchange_order_id 查订单
- 按 carrier_id / symbol / time range 查询
- gate code / blocker code
- order audit logs
- raw request / response 引用
- reconciliation reports
- recovery tasks
- related review

### 当前代码支持什么

- `order_audit_logs` 表存在，且有 order_id、signal_id、created_at、event_type 等索引。
- `execution_intents` 可按 authorization_id 查。
- `orders` 可按 signal_id、symbol、status、parent_order_id 查。
- `positions` 可按 signal_id、symbol 查。
- `brc_campaign_events`、`brc_operations`、`brc_preflight_snapshots` 存在。
- DB 链路支持 authorization → intent → order → audit。
- `/api/brc/audit-trail` 存在。
- `/api/runtime/events` 存在。

### 缺口是什么

- 无订单级审计 API。
- 无 intent chain API。
- 无 authorization chain API。
- DB 链路完整但需要多次查询，无聚合 read model。
- BrcAuditTrailResponse 是否覆盖 order-level 审计需确认。
- 技术审计页需要后端提供链路聚合，不应前端多跳拼接。

### 类型

只读 read model。

### 是否触达交易

否。

### 是否 PG mutation

否。

### 需要主控决策什么

- 是否建设 `/api/trading-console/audit/*` 链路 API。
- order_audit_logs 是否暴露给交易控制台。
- raw payload / raw exchange response 是否允许返回，是否需要脱敏。
- 技术审计页是否优先支持 authorization_id、intent_id、order_id 三种查询。

------

## 10. Carrier Shelf

### 页面需要什么事实

- Carrier 列表
- Carrier 是否可进入实盘行动
- Carrier 对应 symbol / side / cap / max notional
- Carrier 当前是否 blocked
- block 原因：
  - 有仓位
  - 有挂单
  - 有未消费授权
  - 环境错误
  - GKS
  - account facts stale
  - protection / recovery unresolved
- 最近一次实盘结果
- 是否有当前 active authorization / active execution

### 当前代码支持什么

- authorization 中有 carrier_id / strategy_family_id。
- strategy / signal / observation 层有一定基础。
- 旧 StrategyCandidatesV2 存在，但有 sample_data 硬编码风险。

### 缺口是什么

- Carrier Shelf 不应继续依赖 sample_data。
- 需要后端明确 Carrier list / availability read model。
- Carrier 可用性不应由前端推断。
- 是否 P0 需要主控判断；当前交易控制台 P0 更偏账户风险、订单台账、授权执行、恢复复盘。

### 类型

只读 read model。

### 是否触达交易

否。

### 是否 PG mutation

否。

### 需要主控决策什么

- Carrier Shelf 是否纳入当前后端 P0。
- 是否先做当前 active carrier，而不是完整货架。
- sample_data 候选如何隔离或废弃。
- Carrier availability 由哪个服务统一计算。

------

## 11. 信号台账 / TradingView 图表组件

### 页面需要什么事实

信号台账后置，目标形态不是跳转 TradingView，而是内嵌 TradingView / lightweight chart 图表组件，并在图表上展示：

- signal observed
- would-enter observation
- authorization
- entry submitted
- entry filled
- TP placed
- SL placed
- TP/SL filled
- recovery close
- position closed
- review result

### 当前代码支持什么

- `PGSignalORM` / Signal 存在。
- SignalTrack / SignalStatus 存在。
- 实盘观察记录存在。
- 历史信号输出存在。
- signal attempt 存在。
- forward review 表存在。
- marker 所需 timestamp / price / symbol / side 大多可以从多个表获取。
- OHLCV 数据基础存在。
- 但前端无 TradingView / chart 依赖。

### 缺口是什么

- 无统一事件 feed API。
- 无 chart marker API。
- 无 TradingView symbol/timeframe 映射服务。
- 无 chart 组件。
- marker 数据需要聚合 5+ 表。
- 信号台账优先级后置，不应阻塞当前交易控制台 P0。

### 类型

只读 read model + 前端工程增强 + 后续架构治理。

### 是否触达交易

否。

### 是否 PG mutation

否。

### 需要主控决策什么

- 信号台账是否放到 P2。
- 是否采用 lightweight-charts 或 TradingView widget。
- 是否先建设事件 feed API，还是等交易控制台主链完成后再做。
- 信号与订单 / 复盘的关联是否需要补后端字段。

------

## 12. API 清理 / 旧前端隔离

### 页面需要什么事实

交易控制台需要清楚知道哪些 API 可用、哪些已废弃、哪些只用于 dev/testnet/sample。

### 当前代码支持什么

- 当前旧前端页面仍存在。
- `/trial-confirmation` 等旧流程仍有高风险动作。
- StrategyCandidatesV2 存在 sample_data fallback。
- 旧 API、runtime API、BRC console API 混用。

### 缺口是什么

- 缺少 trading-console namespace。
- 缺少 API 分级表：
  - trading-console v1 allowed
  - internal only
  - deprecated
  - testnet/dev only
  - remove candidate
- sample_data 需要从真实主流程隔离。
- 不建议当前直接删除旧 API，应先迁移再清理。

### 类型

架构治理。

### 是否触达交易

清理本身不触达交易。
但涉及高风险旧执行按钮和旧执行 API，需要谨慎。

### 是否 PG mutation

一般否。
如涉及 schema / migration 变更，则另行决策。

### 需要主控决策什么

- 是否建立 `/api/trading-console/*` namespace。
- 是否先只做新 read model，不删除旧 API。
- 是否冻结旧前端执行入口。
- sample_data 是否从真实控制台完全禁止。

------

## 13. 总结：按类型归类

### 只读 read model

- 首页 / dashboard_state
- 账户总览 / account-risk snapshot
- 订单台账 / order-ledger
- 保护健康 / protection-health
- Recovery / recovery-exception state
- 授权状态 / authorization-state
- 执行控制 / execution-control state
- 实盘复盘 / review-state
- 技术审计 / audit-chain
- Carrier Shelf / carrier availability
- 信号台账 / chart event feed

### 安全 action

- 手动触发对账
- void / cancel 未执行授权
- mark manual review required
- retry recovery task
- retry missing protection
- cancel scoped order
- flatten scoped position

其中：

- 手动触发对账风险较低，当前已有 API。
- void 授权是 PG mutation，不触达交易。
- retry protection / cancel order / flatten position 会触达交易，必须主控单独决策。
- 前端不能自行组合交易动作。

### 架构治理

- API namespace 治理
- 旧 API 分级 / 废弃 / 隔离
- sample_data 清理
- fills 表 / fee / funding / slippage / PnL 持久化
- client_order_id 是否作为 nullable 审计增强
- order audit API 暴露
- TradingView / lightweight chart 事件 feed
- 前端旧页面迁移后再删除

------

## 14. 总结：需要主控优先决策的问题

1. 是否接受交易控制台先建设只读 read model 层，再建设安全 action API。
2. 是否建立 `/api/trading-console/*` namespace。
3. 订单台账是否作为后端 P0 read model。
4. 保护健康与 Recovery 状态是否作为 P0 read model。
5. 技术审计链路 API 是否作为 P0。
6. 授权 / 执行控制状态显式化是否作为 P0。
7. Recovery 动作中哪些允许进入前端按钮。
8. fills / fee / funding / PnL 是否作为 P1 架构治理。
9. `client_order_id` 是否降级为 P1/P2 异常恢复增强，而不是 P0。
10. 信号台账 / TradingView 图表是否后置到 P2。
11. 旧 API 是否先标记 deprecated，不立即删除。
12. sample_data 是否从交易控制台真实主流程完全禁止。
# OrderRepository P1 方法测试质量检查清单

> **文档版本**: v1.0  
> **创建日期**: 2026-04-07  
> **QA 负责人**: QA Tester  
> **目标覆盖率**: 75%+  
> **验收状态**: ⏳ 待执行

---

## 📊 测试概览

| 组别 | 方法数 | 用例数 | 目标通过率 | 状态 |
|------|-------|--------|-----------|------|
| **Group A** - 核心查询 | 2 | 17 | 100% | ⏳ 待验收 |
| **Group B** - 过滤查询 | 5 | 19 | 100% | ⏳ 待验收 |
| **Group C** - 别名方法 | 4 | 6 | 100% | ⏳ 待验收 |
| **总计** | 11 | 42 | 100% | ⏳ 待验收 |

---

## 🟢 Group A: 核心查询（17 个用例）

### 1. `get_orders()` - 分页多条件查询（10 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-001** | 无过滤条件查询 | 空参数调用，验证默认行为 | □ 未覆盖 | □ |
| **P1-002** | symbol 过滤 | 单币种过滤 | □ 未覆盖 | □ |
| **P1-003** | status 过滤 | 单状态过滤 | □ 未覆盖 | □ |
| **P1-004** | order_role 过滤 | 单角色过滤 | □ 未覆盖 | □ |
| **P1-005** | 多条件组合过滤 | symbol + status + order_role | □ 未覆盖 | □ |
| **P1-006** | 分页测试 - 第一页 | limit=10, offset=0 | □ 未覆盖 | □ |
| **P1-007** | 分页测试 - 第二页 | limit=10, offset=10 | □ 未覆盖 | □ |
| **P1-008** | 分页边界 - 空结果 | 超出总记录数 | □ 未覆盖 | □ |
| **P1-009** | limit 边界值 | limit=1 | □ 未覆盖 | □ |
| **P1-010** | 空数据库查询 | 无任何订单 | □ 未覆盖 | □ |

**断言检查清单**:
- [ ] 返回结构包含 `items`, `total`, `limit`, `offset`
- [ ] 订单按 `created_at` 降序排列
- [ ] `total` 值为实际总记录数（不受 limit 影响）
- [ ] 分页边界条件验证（offset 超出范围返回空列表）

### 2. `get_orders_by_signal_ids()` - 多信号批量查询（7 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-011** | 单信号查询 | 单个 signal_id | □ 未覆盖 | □ |
| **P1-012** | 多信号批量查询 | 多个 signal_ids | □ 未覆盖 | □ |
| **P1-013** | 带角色过滤 | signal_ids + order_role | □ 未覆盖 | □ |
| **P1-014** | 分页测试 - page=1 | 第一页 | □ 未覆盖 | □ |
| **P1-015** | 分页测试 - page=2 | 第二页 | □ 未覆盖 | □ |
| **P1-016** | 空信号列表 | signal_ids=[] | □ 未覆盖 | □ |
| **P1-017** | 不存在的信号 | signal_ids 不含有效数据 | □ 未覆盖 | □ |

**断言检查清单**:
- [ ] 返回结构包含 `orders`, `total`, `page`, `page_size`
- [ ] `page` 为 1-based 页码
- [ ] 多信号查询结果包含所有信号的订单
- [ ] 角色过滤精确匹配

---

## 🟡 Group B: 过滤查询（19 个用例）

### 3. `get_open_orders()` - 获取未完成订单（4 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-018** | 无币种过滤 | 查询所有 OPEN 订单 | □ 未覆盖 | □ |
| **P1-019** | 币种过滤 | 指定 symbol | □ 未覆盖 | □ |
| **P1-020** | 多币种混合 | 多个币种订单 | □ 未覆盖 | □ |
| **P1-021** | 无 OPEN 订单 | 全部为 FILLED/CANCELLED | □ 未覆盖 | □ |

**关键逻辑验证**:
- [ ] 状态条件：`status IN ('OPEN', 'PARTIALLY_FILLED')`
- [ ] 返回类型为 `List[Order]`
- [ ] 按 `created_at` 降序排列

### 4. `get_orders_by_symbol()` - 按币种查询（4 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-022** | 单币种查询 | 指定 symbol | □ 未覆盖 | □ |
| **P1-023** | limit 限制 | limit=5 | □ 未覆盖 | □ |
| **P1-024** | 不存在的币种 | symbol 无数据 | □ 未覆盖 | □ |
| **P1-025** | 排序验证 | 多订单 | □ 未覆盖 | □ |

**断言检查清单**:
- [ ] 返回类型为 `List[Order]`
- [ ] 只返回指定币种订单
- [ ] 返回数量不超过 limit
- [ ] 按 `created_at` 降序排列

### 5. `get_orders_by_role()` - 按角色查询（5 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-026** | 单角色查询 | ENTRY 角色 | □ 未覆盖 | □ |
| **P1-027** | 组合 signal_id 过滤 | role + signal_id | □ 未覆盖 | □ |
| **P1-028** | 组合 symbol 过滤 | role + symbol | □ 未覆盖 | □ |
| **P1-029** | 三重过滤 | role + signal_id + symbol | □ 未覆盖 | □ |
| **P1-030** | 空结果 | 无匹配数据 | □ 未覆盖 | □ |

**断言检查清单**:
- [ ] 返回类型为 `List[Order]`
- [ ] 所有过滤条件精确匹配
- [ ] 空结果返回空列表

### 6. `get_by_status()` - 按状态查询（3 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-031** | 单状态查询 | FILLED 状态 | □ 未覆盖 | □ |
| **P1-032** | 排序验证 | 多订单 | □ 未覆盖 | □ |
| **P1-033** | 空结果 | 无匹配状态 | □ 未覆盖 | □ |

**验收标准**:
- [ ] 验证与 `get_orders_by_status()` 行为一致
- [ ] 返回类型为 `List[Order]`
- [ ] 按 `created_at` 降序排列

### 7. `mark_order_filled()` - 标记成交（3 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-034** | 正常标记成交 | OPEN 订单 → FILLED | □ 未覆盖 | □ |
| **P1-035** | 验证 updated_at 更新 | 标记后检查 | □ 未覆盖 | □ |
| **P1-036** | 不存在的订单 | order_id 无效 | □ 未覆盖 | □ |

**关键逻辑验证**:
- [ ] 状态变更为 `FILLED`
- [ ] `filled_at` 字段设置
- [ ] `updated_at` 时间戳更新
- [ ] 不存在的订单不抛异常

---

## 🔵 Group C: 别名方法和辅助方法（6 个用例）

### 8. `save_order()` - 保存订单别名（2 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-037** | 保存新订单 | 验证委托给 `save()` | □ 未覆盖 | □ |
| **P1-038** | 更新已存在订单 | UPSERT 行为 | □ 未覆盖 | □ |

**验收标准**:
- [ ] 验证与 `save()` 行为完全一致
- [ ] UPSERT 逻辑正确（ON CONFLICT DO UPDATE）

### 9. `get_order_detail()` - 获取订单详情别名（2 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-039** | 查询存在订单 | 验证委托给 `get_order()` | □ 未覆盖 | □ |
| **P1-040** | 查询不存在订单 | order_id 无效 | □ 未覆盖 | □ |

**验收标准**:
- [ ] 验证与 `get_order()` 行为完全一致
- [ ] 不存在订单返回 `None`

### 10. `get_by_signal_id()` - 按信号查询别名（2 个用例）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P1-041** | 查询存在信号 | 验证委托给 `get_orders_by_signal()` | □ 未覆盖 | □ |
| **P1-042** | 查询不存在信号 | signal_id 无效 | □ 未覆盖 | □ |

**验收标准**:
- [ ] 验证与 `get_orders_by_signal()` 行为完全一致
- [ ] 空结果返回空列表

### 11. `get_order_count()` - 计数方法（已在 P0 覆盖，验证即可）

| 用例 ID | 场景 | 测试要点 | 覆盖状态 | 验收 |
|--------|------|---------|----------|------|
| **P0-015** | 正常计数 | 5 个订单 | ✅ 已覆盖 | ✅ |
| **P0-016** | 空结果 | 无订单 | ✅ 已覆盖 | ✅ |

**状态**: ✅ 已有测试，无需新增

---

## 📋 分页逻辑边界值检查

| 边界场景 | 测试用例 | 验证要点 | 覆盖状态 |
|---------|---------|---------|----------|
| offset=0 | P1-006 | 第一页第一条 | □ 未覆盖 |
| offset=总记录数 | P1-008 | 空结果 | □ 未覆盖 |
| offset>总记录数 | P1-008 | 空结果 | □ 未覆盖 |
| limit=1 | P1-009 | 最小 limit | □ 未覆盖 |
| limit=默认值 50 | P1-001 | 默认行为 | □ 未覆盖 |
| page=1 (1-based) | P1-014 | 第一页 | □ 未覆盖 |
| page=2 | P1-015 | 第二页 | □ 未覆盖 |
| page 超出范围 | P1-017 | 空结果 | □ 未覆盖 |

---

## 🔀 过滤条件组合检查

| 组合场景 | 测试用例 | 过滤条件 | 覆盖状态 |
|---------|---------|---------|----------|
| 单条件 - symbol | P1-002 | symbol only | □ 未覆盖 |
| 单条件 - status | P1-003 | status only | □ 未覆盖 |
| 单条件 - order_role | P1-004 | order_role only | □ 未覆盖 |
| 双条件 - symbol+status | P1-005 | symbol + status | □ 未覆盖 |
| 双条件 - symbol+role | P1-028 | symbol + role | □ 未覆盖 |
| 双条件 - signal_id+role | P1-027 | signal_id + role | □ 未覆盖 |
| 三条件 - symbol+status+role | P1-005 | symbol + status + role | □ 未覆盖 |
| 三条件 - role+signal_id+symbol | P1-029 | role + signal_id + symbol | □ 未覆盖 |

---

## 🔄 别名方法委托验证检查

| 别名方法 | 委托目标 | 验证测试 | 覆盖状态 |
|---------|---------|---------|----------|
| `save_order()` | `save()` | P1-037, P1-038 | □ 未覆盖 |
| `get_order_detail()` | `get_order()` | P1-039, P1-040 | □ 未覆盖 |
| `get_by_signal_id()` | `get_orders_by_signal()` | P1-041, P1-042 | □ 未覆盖 |
| `get_by_status()` | `get_orders_by_status()` | P1-031, P1-032, P1-033 | □ 未覆盖 |

---

## ✅ 验收标准

### 功能验收
- [ ] 所有 P1 方法单元测试覆盖率 100%
- [ ] 分页逻辑 100% 覆盖（边界值验证）
- [ ] 过滤条件组合测试 100% 覆盖
- [ ] 别名方法委托验证 100% 覆盖

### 质量验收
- [ ] 所有测试用例命名规范（P1-XXX 格式）
- [ ] 测试代码通过 lint 检查
- [ ] 无重复测试逻辑
- [ ] 测试数据隔离（使用临时数据库）

### 覆盖率验收
| 指标 | 当前 | 目标 | 验收 |
|-----|------|------|------|
| 整体覆盖率 | 33% | 75%+ | □ |
| P1 方法覆盖率 | 0% | 100% | □ |
| 分支覆盖率 | - | 85%+ | □ |

### Group 通过率验收
| 组别 | 用例数 | 目标通过率 | 实际通过 | 验收 |
|------|-------|-----------|---------|------|
| Group A | 17 | 100% | -% | □ |
| Group B | 19 | 100% | -% | □ |
| Group C | 6 | 100% | -% | □ |
| **总计** | **42** | **100%** | **-%** | □ |

---

## 📝 测试执行记录

### 第一次执行
- **执行日期**: _________
- **执行人**: _________
- **通过率**: ___%
- **失败用例**: _________
- **备注**: _________

### 修复后复测
- **复测日期**: _________
- **复测人**: _________
- **通过率**: ___%
- **遗留问题**: _________

---

## 🔧 问题跟踪

| 问题 ID | 描述 | 严重级别 | 状态 | 修复日期 |
|--------|------|---------|------|---------|
| - | - | - | - | - |

---

*最后更新：2026-04-07*
*下次更新：P1 测试执行完成后*

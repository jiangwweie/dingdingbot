# S5-2 任务执行文档 - 信号 pending 状态跟踪

**任务 ID**: S5-2
**阶段**: Phase 5 - 窗口 2
**预估工时**: 6 小时
**优先级**: 中

---

## 任务概述

跟踪信号从生成到成交的全流程状态，提供状态查询 API 和前端展示。

### 当前状态

现有代码中，信号生成后直接落库，没有状态跟踪机制：
- `SignalResult` - 信号结果模型
- `signal_repository.save()` - 信号落库
- 无状态追踪

### 目标状态

```
信号生成 → GENERATED
    │
    ▼
等待成交 → PENDING
    │
    ├──→ 已成交 → FILLED
    ├──→ 已取消 → CANCELLED
    └──→ 被拒绝 → REJECTED
```

---

## 执行步骤（按顺序完成）

### 步骤 1: 信号状态机模型设计

**修改文件**: `src/domain/models.py`

**新增枚举**:

```python
from enum import Enum


class SignalStatus(str, Enum):
    """信号状态枚举"""
    GENERATED = "generated"      # 已生成
    PENDING = "pending"          # 等待成交
    FILLED = "filled"            # 已成交
    CANCELLED = "cancelled"      # 已取消
    REJECTED = "rejected"        # 被拒绝
```

**新增模型**:

```python
class SignalTrack(BaseModel):
    """信号全生命周期跟踪"""
    signal_id: str
    original_signal: SignalResult
    status: SignalStatus
    created_at: int  # 毫秒时间戳
    updated_at: int  # 毫秒时间戳
    filled_price: Optional[Decimal] = None
    filled_at: Optional[int] = None
    reject_reason: Optional[str] = None
    cancel_reason: Optional[str] = None

    class Config:
        use_enum_values = True
```

**验收标准**:
- 类型检查通过
- 模型字段完整

---

### 步骤 2: 实现 SignalStatusTracker

**创建文件**: `src/application/signal_tracker.py`

**完整实现**:

```python
"""
Signal Status Tracker - 信号状态跟踪器

跟踪信号从生成到成交的全流程状态。
"""
import asyncio
import time
from typing import Dict, Optional, List
from decimal import Decimal

from src.domain.models import SignalResult, SignalTrack, SignalStatus
from src.infrastructure.signal_repository import SignalRepository
from src.infrastructure.logger import logger


class SignalStatusTracker:
    """
    信号状态跟踪器

    功能:
    - 跟踪信号状态变化
    - 提供状态查询接口
    - 记录状态变更历史
    """

    def __init__(self, repository: SignalRepository):
        self._repository = repository
        self._in_memory_cache: Dict[str, SignalTrack] = {}
        self._lock = asyncio.Lock()

    async def track_signal(self, signal: SignalResult) -> str:
        """
        开始跟踪信号

        Args:
            signal: 信号结果对象

        Returns:
            signal_id: 信号唯一标识
        """
        signal_id = self._generate_signal_id(signal)

        now = int(time.time() * 1000)
        track = SignalTrack(
            signal_id=signal_id,
            original_signal=signal,
            status=SignalStatus.GENERATED,
            created_at=now,
            updated_at=now,
        )

        async with self._lock:
            self._in_memory_cache[signal_id] = track

        logger.info(f"开始跟踪信号：{signal_id} ({signal.symbol} {signal.timeframe})")
        return signal_id

    async def update_status(
        self,
        signal_id: str,
        status: SignalStatus,
        filled_price: Optional[Decimal] = None,
        reason: Optional[str] = None,
    ) -> None:
        """
        更新信号状态

        Args:
            signal_id: 信号标识
            status: 新状态
            filled_price: 成交价格（仅 FILLED 状态需要）
            reason: 状态变更原因
        """
        async with self._lock:
            if signal_id not in self._in_memory_cache:
                logger.warning(f"信号不存在：{signal_id}")
                return

            track = self._in_memory_cache[signal_id]
            track.status = status
            track.updated_at = int(time.time() * 1000)

            if status == SignalStatus.FILLED:
                track.filled_price = filled_price
                track.filled_at = track.updated_at
            elif status == SignalStatus.REJECTED:
                track.reject_reason = reason
            elif status == SignalStatus.CANCELLED:
                track.cancel_reason = reason

            logger.info(f"信号状态更新：{signal_id} -> {status.value}")

    async def get_signal_status(self, signal_id: str) -> Optional[SignalTrack]:
        """
        查询信号状态

        Args:
            signal_id: 信号标识

        Returns:
            SignalTrack 对象或 None
        """
        async with self._lock:
            return self._in_memory_cache.get(signal_id)

    async def list_statuses(
        self,
        status_filter: Optional[SignalStatus] = None,
        limit: int = 50,
    ) -> List[SignalTrack]:
        """
        批量查询信号状态

        Args:
            status_filter: 状态过滤
            limit: 结果数量限制

        Returns:
            SignalTrack 列表
        """
        async with self._lock:
            tracks = list(self._in_memory_cache.values())

        if status_filter:
            tracks = [t for t in tracks if t.status == status_filter]

        # 按创建时间倒序
        tracks.sort(key=lambda t: t.created_at, reverse=True)

        return tracks[:limit]

    def _generate_signal_id(self, signal: SignalResult) -> str:
        """生成信号唯一 ID"""
        import hashlib
        import time

        unique_str = f"{signal.symbol}{signal.timeframe}{signal.timestamp}{time.time()}"
        return hashlib.sha256(unique_str.encode()).hexdigest()[:16]
```

**验收标准**:
- 单元测试通过
- 状态变更逻辑正确

---

### 步骤 3: 信号落库集成

**修改文件**:
- `src/application/signal_pipeline.py`
- `src/infrastructure/signal_repository.py`

#### 3.1 修改 SignalPipeline

```python
# 在 SignalPipeline 类中添加

from src.application.signal_tracker import SignalStatusTracker

class SignalPipeline:
    def __init__(self, ...):
        # ... 现有初始化代码
        self._status_tracker = SignalStatusTracker(self._repository)

    async def process_kline(self, kline: KlineData) -> None:
        # ... 现有处理逻辑

        # 当生成信号时，开始跟踪
        if signal_result:
            signal_id = await self._status_tracker.track_signal(signal_result)
            # 更新为 PENDING 状态
            await self._status_tracker.update_status(
                signal_id,
                SignalStatus.PENDING
            )
```

#### 3.2 修改 SignalRepository

```python
# 在 signal_repository.py 中添加状态字段存储

async def save(self, signal: SignalResult, status: str = "generated") -> str:
    """
    保存信号到数据库

    Args:
        signal: 信号结果
        status: 信号状态

    Returns:
        signal_id: 生成的信号 ID
    """
    # ... 现有保存逻辑
    # INSERT 语句添加 status 字段
```

**验收标准**:
- 信号生成时自动初始化为 GENERATED
- 状态正确流转

---

### 步骤 4: 状态查询 API

**修改文件**: `src/interfaces/api.py`

**新增端点**:

```python
from src.domain.models import SignalStatus


@app.get("/api/signals/{signal_id}/status", response_model=SignalTrack)
async def get_signal_status(signal_id: str) -> SignalTrack:
    """
    查询单个信号状态

    Args:
        signal_id: 信号标识

    Returns:
        信号状态信息

    Raises:
        HTTPException: 404 信号不存在
    """
    tracker = app.state.signal_tracker  # 需要从应用状态获取
    track = await tracker.get_signal_status(signal_id)

    if not track:
        raise HTTPException(status_code=404, detail="信号不存在")

    return track


@app.get("/api/signals/status", response_model=List[SignalTrack])
async def list_signal_statuses(
    status: Optional[SignalStatus] = Query(None),
    limit: int = Query(50, ge=1, le=100),
) -> List[SignalTrack]:
    """
    批量查询信号状态

    Args:
        status: 状态过滤
        limit: 结果数量限制

    Returns:
        信号状态列表
    """
    tracker = app.state.signal_tracker
    return await tracker.list_statuses(status_filter=status, limit=limit)
```

**验收标准**:
- API 返回正确数据
- 状态过滤正常

---

### 步骤 5: 前端 UI 实现

#### 5.1 更新类型定义

**修改文件**: `web-front/src/lib/api.ts`

```typescript
export interface SignalTrack {
  signal_id: string;
  original_signal: SignalResult;
  status: SignalStatus;
  created_at: number;
  updated_at: number;
  filled_price?: string;
  filled_at?: number;
  reject_reason?: string;
  cancel_reason?: string;
}

export enum SignalStatus {
  GENERATED = 'generated',
  PENDING = 'pending',
  FILLED = 'filled',
  CANCELLED = 'cancelled',
  REJECTED = 'rejected',
}

// 新增 API 调用函数
export async function getSignalStatus(signalId: string): Promise<SignalTrack> {
  const response = await fetch(`/api/signals/${signalId}/status`);
  if (!response.ok) {
    throw new Error('Failed to fetch signal status');
  }
  return response.json();
}

export async function listSignalStatuses(
  status?: SignalStatus,
  limit = 50
): Promise<SignalTrack[]> {
  const params = new URLSearchParams();
  if (status) params.append('status', status);
  params.append('limit', limit.toString());

  const response = await fetch(`/api/signals/status?${params}`);
  if (!response.ok) {
    throw new Error('Failed to fetch signal statuses');
  }
  return response.json();
}
```

#### 5.2 创建状态徽章组件

**创建文件**: `web-front/src/components/SignalStatusBadge.tsx`

```typescript
import React from 'react';
import { SignalStatus } from '../lib/api';

interface SignalStatusBadgeProps {
  status: SignalStatus;
}

const statusConfig: Record<SignalStatus, { label: string; color: string }> = {
  [SignalStatus.GENERATED]: { label: '已生成', color: '#6b7280' },      // 灰色
  [SignalStatus.PENDING]: { label: '等待成交', color: '#eab308' },       // 黄色
  [SignalStatus.FILLED]: { label: '已成交', color: '#22c55e' },         // 绿色
  [SignalStatus.CANCELLED]: { label: '已取消', color: '#3b82f6' },      // 蓝色
  [SignalStatus.REJECTED]: { label: '被拒绝', color: '#ef4444' },       // 红色
};

export function SignalStatusBadge({ status }: SignalStatusBadgeProps) {
  const config = statusConfig[status];

  return (
    <span
      className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium"
      style={{
        backgroundColor: `${config.color}20`,  // 20% 透明度背景
        color: config.color,
      }}
    >
      <span
        className="w-1.5 h-1.5 rounded-full mr-1.5"
        style={{ backgroundColor: config.color }}
      />
      {config.label}
    </span>
  );
}
```

#### 5.3 集成到信号列表

**修改文件**: `web-front/src/pages/Signals.tsx` (或类似文件)

```typescript
import { SignalStatusBadge } from '../components/SignalStatusBadge';

// 在信号列表中添加状态列
function SignalsList({ signals }: { signals: SignalTrack[] }) {
  return (
    <table>
      <thead>
        <tr>
          <th>币种</th>
          <th>周期</th>
          <th>方向</th>
          <th>入场价</th>
          <th>状态</th>
          <th>创建时间</th>
        </tr>
      </thead>
      <tbody>
        {signals.map((signal) => (
          <tr key={signal.signal_id}>
            <td>{signal.original_signal.symbol}</td>
            <td>{signal.original_signal.timeframe}</td>
            <td>{signal.original_signal.direction}</td>
            <td>{signal.original_signal.entry_price}</td>
            <td>
              <SignalStatusBadge status={signal.status} />
            </td>
            <td>{formatDate(signal.created_at)}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

**验收标准**:
- TypeScript 编译通过
- 状态徽章显示正确
- 颜色区分明显

---

## 验收标准

### 功能验收
- [ ] 信号生成时状态为 GENERATED
- [ ] 状态可以正确流转
- [ ] API 返回正确状态数据
- [ ] 前端显示状态徽章

### 代码验收
- [ ] 类型检查通过
- [ ] 所有测试通过
- [ ] 代码已提交 Git

### 文档验收
- [ ] 代码注释完整
- [ ] 更新 CHANGELOG

---

## 相关文件

| 文件 | 操作 |
|------|------|
| `src/domain/models.py` | 修改 - 新增状态枚举和模型 |
| `src/application/signal_tracker.py` | 创建 - 状态跟踪器 |
| `src/application/signal_pipeline.py` | 修改 - 集成跟踪器 |
| `src/infrastructure/signal_repository.py` | 修改 - 新增状态字段 |
| `src/interfaces/api.py` | 修改 - 新增查询 API |
| `web-front/src/lib/api.ts` | 修改 - 新增类型和调用 |
| `web-front/src/components/SignalStatusBadge.tsx` | 创建 - 状态徽章组件 |
| `web-front/src/pages/Signals.tsx` | 修改 - 集成状态显示 |

---

## 开始执行

**启动命令**（仅供参考，无需实际运行）:

```bash
# 1. 阅读现有代码
cat src/domain/models.py
cat src/infrastructure/signal_repository.py

# 2. 实现状态模型
# 3. 创建跟踪器
# 4. 实现 API
# 5. 前端 UI
# 6. 运行测试
pytest tests/unit/test_signal_tracker.py -v

# 7. 前端编译
cd web-front && npm run build

# 8. 提交 Git
git add -A
git commit -m "feat(S5-2): 实现信号状态跟踪"
```

---

**开始执行任务！按顺序完成每个步骤，完成后标记验收标准。**

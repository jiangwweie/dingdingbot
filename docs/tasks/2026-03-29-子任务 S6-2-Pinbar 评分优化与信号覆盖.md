# 子任务 S6-2: Pinbar 评分优化与信号覆盖

**创建日期**: 2026-03-29
**更新日期**: 2026-03-29（协调者模式完善）
**优先级**: 高
**预计工作量**: 12-20 小时

---

## 背景与目标

### 问题背景（用户原始反馈）

用户回测发现：
1. 03-21 21:00 出现看空 Pinbar（Score 0.727，十字星型，波幅 116 USDT）
2. 03-21 22:00 出现看空 Pinbar（Score 0.715，真突破型，波幅 527 USDT）

**数据分析结论**：
- 两个 Pinbar 都满足几何条件（wick_ratio ≥ 0.6，body_ratio ≤ 0.3）
- 21:00 的评分更高（0.727 > 0.715），但实际是"十字星"，交易价值低
- 22:00 的评分略低，但波幅是 4.5 倍，实际交易价值更高

**当前问题**：
- 评分只看几何比例（影线占比），无法区分"十字星"和"真突破"
- 低质量信号（十字星）可能获得高分
- 更高质量的信号出现时，没有覆盖/替代机制

### 业务目标

1. **过滤低质量信号**：使用 ATR 门槛（min_atr_ratio=0.5）过滤十字星/一字线
2. **优化评分逻辑**：加入 ATR 归一化因子，反映实际波幅
3. **信号覆盖机制**：同方向更高分信号出现时，自动替代旧信号
4. **通知增强**：覆盖时明确告知用户，反向信号时提示风险

---

## 现状分析（架构摸底）

### 已有组件 ✅

| 组件 | 位置 | 状态 |
|------|------|------|
| `AtrFilterDynamic` | `src/domain/filter_factory.py:332` | ✅ 已实现，默认 enabled=false |
| `FilterBase` 抽象类 | `src/domain/filter_factory.py:49` | ✅ 统一接口 |
| `SignalStatus` 枚举 | `src/domain/models.py:38` | ⚠️ 需扩展（添加 ACTIVE/SUPERSEDED） |
| `process_kline()` | `src/application/signal_pipeline.py:352` | ⚠️ 需整合覆盖逻辑 |
| 冷却期逻辑 | `src/application/signal_pipeline.py:411-425` | ⚠️ 需修改为支持覆盖 |

### 需要扩展的 ⚠️

| 组件 | 修改内容 |
|------|----------|
| `SignalStatus` | 添加 `ACTIVE`、`SUPERSEDED` 状态 |
| `PinbarStrategy.detect()` | 添加 ATR 评分调整逻辑 |
| `signal_repository` | 添加 `superseded_by` 等字段和操作 |
| `process_kline()` | 添加覆盖检查和通知增强 |
| `notifier.send_signal()` | 添加覆盖/反向说明 |
| 前端 `Signals.tsx` | 支持新状态渲染 |

---

## 核心规则

### 1. ATR 门槛判定

```yaml
min_atr_ratio: 0.5  # K 线波幅必须 ≥ ATR × 0.5
atr_period: 14       # ATR 计算周期（业界标准）
```

**判定逻辑**：
```
candle_range = high - low
atr_ratio = candle_range / atr

if atr_ratio < 0.5:
    直接过滤，不产生信号
```

### 2. 评分公式

```python
# 基础分（几何质量）
base_score = wick_ratio  # 0~1

# ATR 加分（波幅质量）
atr_bonus = min(atr_ratio, 2.0) × 0.3

# 最终评分
final_score = base_score × 0.7 + atr_bonus
# 上限 1.0
```

**效果对比**（假设 ATR=400 USDT）：

| K 线 | wick_ratio | atr_ratio | 原始 Score | 调整后 Score |
|------|------------|-----------|------------|--------------|
| 21:00 十字星 | 0.727 | 0.29 | 0.727 | **被过滤** (0.29<0.5) |
| 22:00 真突破 | 0.715 | 1.32 | 0.715 | **0.78** |

### 3. 覆盖规则

| 条件 | 行为 |
|------|------|
| 同 symbol:timeframe:direction | 才考虑覆盖 |
| 新信号 Score > 旧信号 Score | 标记旧为 SUPERSEDED，推送新的 |
| 新信号 Score ≤ 旧信号 Score | 仍推送新的（不覆盖） |
| 反方向信号 | 不覆盖，推送时带对立提示 |

### 4. 推送频率

- **每次都推送**（除非被 ATR 过滤）
- 高分覆盖时，通知中特别说明

---

## 任务分解（调整版）

### S6-2-1: ATR 过滤器配置与集成 ⚠️ 已有实现，需配置

**现状**: `AtrFilterDynamic` 类已存在于 `filter_factory.py:332`

**需要做的**:
1. 在 `config/core.yaml` 中添加配置:
   ```yaml
   atr_filter:
     enabled: true
     period: 14
     min_atr_ratio: 0.5  # 最小 ATR 倍数（波幅/ATR）
   ```

2. 在 `FilterFactory` 中注册（检查是否已注册）

3. 验证 ATR 计算逻辑（Wilder 平滑法 vs 简单平均）
   - 当前实现使用简单平均（第 389 行）
   - 建议改用 Wilder 平滑法：`ATR = (prev_ATR × 13 + current_TR) / 14`

**验收**:
- [ ] ATR 计算与 TradingView 一致
- [ ] min_atr_ratio=0.5 时，十字星被正确过滤
- [ ] 单元测试验证

---

### S6-2-2: 评分公式优化 (ATR 调整)

**文件**: `src/domain/strategy_engine.py`

**修改**: `PinbarStrategy.detect()` 方法

**现状**:
- 当前评分逻辑在第 218 行：`score = float(wick_ratio)`
- 仅使用影线占比，未考虑绝对波幅

**修改方案**:
```python
# 入参增加 atr_value
def detect(self, kline: KlineData, atr_value: Optional[Decimal] = None) -> Optional[PatternResult]:
    # ... 现有检测逻辑 ...

    # 新增：ATR 调整评分
    if atr_value and atr_value > 0:
        candle_range = kline.high - kline.low
        atr_ratio = candle_range / atr_value

        # 基础分（几何质量）
        base_score = float(wick_ratio)

        # ATR 加分（波幅质量），上限 2 倍
        atr_bonus = min(float(atr_ratio), 2.0) * 0.3

        # 最终评分
        score = base_score * 0.7 + atr_bonus
        score = min(score, 1.0)  # 上限 1.0
    else:
        score = float(wick_ratio)  # 降级为原始逻辑
```

**集成点**: 在 `signal_pipeline.py:_run_strategy()` 调用时传入 ATR 值

---

### S6-2-3: 数据库字段扩展

**文件**: `src/infrastructure/signal_repository.py`

**新增字段** (`signals` 表):

```sql
ALTER TABLE signals ADD COLUMN superseded_by TEXT;
ALTER TABLE signals ADD COLUMN opposing_signal_id TEXT;
ALTER TABLE signals ADD COLUMN opposing_signal_score REAL;
```

**迁移逻辑**（Python）:
```python
# 在 __init__ 数据库初始化时
for column, default_type in [
    ("superseded_by", "TEXT"),
    ("opposing_signal_id", "TEXT"),
    ("opposing_signal_score", "REAL"),
]:
    try:
        await self._db.execute(f"ALTER TABLE signals ADD COLUMN {column} {default_type}")
    except aiosqlite.OperationalError as e:
        if "duplicate column name" not in str(e).lower():
            raise
```

**方法扩展**:
```python
async def update_signal_status(self, signal_id: str, status: str):
    """更新信号状态（支持 SUPERSEDED）"""
    pass

async def update_superseded_by(self, signal_id: str, superseded_by: str):
    """标记信号被哪个信号替代"""
    pass

async def get_active_signal(self, dedup_key: str) -> Optional[dict]:
    """获取指定 key 的 ACTIVE 状态信号"""
    # WHERE symbol=? AND timeframe=? AND direction=? AND status='ACTIVE'
    pass

async def get_opposing_signal(
    self, symbol: str, timeframe: str, direction: Direction
) -> Optional[dict]:
    """获取对立方向的 ACTIVE 信号"""
    # WHERE symbol=? AND timeframe=? AND direction!=? AND status='ACTIVE'
    pass
```

---

### S6-2-4: 信号覆盖逻辑（整合冷却期）

**文件**: `src/application/signal_pipeline.py`

**修改**: `process_kline()` 方法（从第 352 行开始）

**现状分析**:
- 当前冷却期逻辑在第 411-425 行
- `dedup_key` 包含 `strategy_name`，允许不同策略独立触发
- 冷却期内信号被跳过，不推送也不落库

**修改方案**:
```python
# 在 Step: Signal deduplication 处修改（约 417 行）
if now - last_fired < self._cooldown_seconds:
    # 现有逻辑：直接跳过
    # 新增：检查新信号是否更优
    if score > last_signal_score:  # 需要实现
        # 新信号更优，覆盖旧信号
        await self._repository.update_signal_status(last_signal_id, "SUPERSEDED")
        await self._repository.update_superseded_by(last_signal_id, signal_id)
        # 继续推送新信号，带覆盖说明
        superseded_signal = last_signal
    else:
        # 新信号不更优，跳过
        continue
```

**注意**: 需要在冷却缓存中存储更多信息（signal_id, score）

---

### S6-2-5: 通知消息增强

**文件**: `src/infrastructure/notifier.py`

**修改**: `send_signal()` 方法

**新增参数**:
```python
async def send_signal(
    self,
    signal: SignalResult,
    superseded_signal: Optional[SignalResult] = None,
    opposing_score: Optional[float] = None,
):
```

**消息模板**:
- 覆盖场景：`🔥 **信号更新 - 更优品质** 🔥`
- 反向场景：`⚠️ **反向信号提醒** ⚠️`
- 普通场景：`📈 **新信号**`

---

### S6-2-6: 前端信号列表增强

**文件**: `web-front/src/pages/Signals.tsx`

**状态枚举扩展**:
```typescript
enum SignalStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUPERSEDED = 'superseded',
  FILLED = 'filled',
  CANCELLED = 'cancelled',
  WON = 'won',
  LOST = 'lost',
}
```

**UI 增强**:
- `SUPERSEDED` 信号显示"已被替代"标记
- 存在 `opposing_signal_score` 时显示"对立信号"提示
- 筛选器新增"仅看有效信号"选项（隐藏 SUPERSEDED）

---

## 依赖关系

```
S6-2-1 (ATR 过滤器配置)
    ↓
S6-2-2 (评分优化)
    ↓
S6-2-3 (数据库扩展)
    ↓
S6-2-4 (覆盖逻辑)
    ↓
S6-2-5 (通知增强)
    ↓
S6-2-6 (前端增强)
```

---

## 验收标准

### ATR 过滤器 (S6-2-1)
- [ ] ATR 计算正确（与 TradingView 一致）
- [ ] atr_ratio < 0.5 的 K 线被正确过滤
- [ ] 单元测试覆盖率 100%

### 评分优化 (S6-2-2)
- [ ] 高 ATR 比值 K 线获得更高评分
- [ ] 评分上限为 1.0
- [ ] 无 ATR 时降级为原始逻辑

### 数据库扩展 (S6-2-3)
- [ ] 迁移脚本不报错（兼容旧数据库）
- [ ] 新增字段可正确读写

### 覆盖逻辑 (S6-2-4)
- [ ] 同方向高分信号正确替代旧信号
- [ ] 旧信号状态变为 SUPERSEDED
- [ ] 反向信号不覆盖

### 通知增强 (S6-2-5)
- [ ] 覆盖时消息包含"更优品质"说明
- [ ] 反向信号消息包含对立提示
- [ ] 飞书消息格式正确

### 前端增强 (S6-2-6)
- [ ] SUPERSEDED 信号正确显示
- [ ] 对立信号标记正确显示
- [ ] 筛选功能正常工作

---

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| ATR 计算性能 | 高频率 K 线更新可能卡顿 | 使用缓存，仅在最新 K 线闭合时计算 |
| 数据库迁移失败 | 旧数据丢失 | 迁移前备份，使用 IF NOT EXISTS |
| 并发覆盖竞争 | 两个信号同时进入 | 使用 asyncio.Lock 保护 |
| 通知轰炸 | 短时间内多条推送 | 保留冷却期逻辑，但按方向独立 |

---

## 相关文件

- `src/domain/filter_factory.py` - ATR 过滤器已有实现
- `src/domain/strategy_engine.py` - PinbarStrategy 评分逻辑
- `src/domain/models.py` - SignalStatus 枚举
- `src/application/signal_pipeline.py` - 覆盖逻辑整合
- `src/infrastructure/signal_repository.py` - 数据库字段扩展
- `src/infrastructure/notifier.py` - 通知增强
- `web-front/src/pages/Signals.tsx` - 前端 UI 增强
- `config/core.yaml` - ATR 过滤器配置
- `tests/unit/test_filter_factory.py` - ATR 测试
- `tests/unit/test_strategy_engine.py` - 评分测试

---

## 决策记录

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| min_atr_ratio | 0.3 / 0.5 / 1.0 | 0.5 | 平衡过滤和机会 |
| ATR 周期 | 14 / 20 / 可配置 | 14 | 业界标准 |
| 覆盖方向 | 严格 / 宽松 | 严格（同方向） | 逻辑清晰，避免误操作 |
| 推送频率 | 每次 / 仅最优 / 可配置 | 每次 | 信息完整 |
| 历史信号 | 删除 / 隐藏 / 标记 | 标记 (SUPERSEDED) | 保持可追溯性 |
| ATR 计算 | Wilder / 简单平均 | Wilder（建议） | 与 TradingView 一致 |

---

## 会话历史与参考

**会话参考**: 2026-03-29 会话

**关键分析**:
- 用户提供的两个 Pinbar 案例数据分析
- 21:00 十字星：Score 0.727，波幅 116 USDT，ATR 比值 0.29
- 22:00 真突破：Score 0.715，波幅 527 USDT，ATR 比值 1.32

**用户决策**:
1. min_atr_ratio = 0.5
2. ATR 周期采用业界标准 14
3. 严格按方向覆盖（LONG 只覆盖 LONG）
4. 每次都推送，高分覆盖时特别说明
5. 历史信号标记 SUPERSEDED，不删除

---

*文档结束*

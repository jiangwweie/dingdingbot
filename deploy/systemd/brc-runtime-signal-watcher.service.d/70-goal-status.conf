[Service]
# Retired as a final goal-status writer.
# The final StrategyGroup runtime goal summary is written by
# 80-product-state-refresh.conf after Candidate Pool, Daily Table, and Single
# Lane Packet are refreshed through the DB-backed production current path. This
# drop-in intentionally has no ExecStartPost writer so legacy pilot/status scope
# alignment cannot overwrite the final PG-backed goal status.

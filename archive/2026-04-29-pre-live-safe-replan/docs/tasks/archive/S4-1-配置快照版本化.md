# S4-1: 配置快照版本化（Rollback）

**任务负责人**: 用户
**预估工时**: 6-8 小时
**优先级**: 中
**依赖**: 无（独立任务）

---

## 目标

实现配置快照版本化功能，支持：
1. 保存当前配置为快照
2. 查看历史快照列表
3. 回滚到任意历史版本

---

## 交付物

### 后端文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/domain/models.py` | 修改 | 新增 `ConfigSnapshot` Pydantic 模型 |
| `src/infrastructure/signal_repository.py` | 修改 | 新增快照 CRUD 方法 |
| `src/interfaces/api.py` | 修改 | 新增快照管理端点 |

### 前端文件（可选）
| 文件 | 操作 | 说明 |
|------|------|------|
| `gemimi-web-front/src/components/ConfigSnapshots.tsx` | 新建 | 快照管理 UI 组件 |
| `gemimi-web-front/src/lib/api.ts` | 修改 | 新增快照 API 调用函数 |

### 测试文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `tests/unit/test_config_snapshot.py` | 新建 | 单元测试 |

---

## 实现步骤

### 步骤 1: 创建 ConfigSnapshot 模型

**文件**: `src/domain/models.py`

```python
class ConfigSnapshot(BaseModel):
    """Configuration snapshot for version control and rollback."""
    id: Optional[int] = None
    version: str = Field(..., description="Version tag, e.g., 'v1.0.0'")
    config_json: str = Field(..., description="Serialized UserConfig JSON")
    description: str = Field(default="", description="Snapshot description")
    created_at: str = Field(..., description="Creation timestamp (ISO 8601)")
    created_by: str = Field(default="user", description="Creator identifier")

    # Index fields for quick lookup
    is_active: bool = Field(default=False, description="Whether this snapshot is currently active")
```

**验收标准**:
- [ ] 模型字段完整
- [ ] 支持 JSON 序列化
- [ ] 可通过 Pydantic 验证

---

### 步骤 2: 创建数据库表

**文件**: `src/infrastructure/signal_repository.py`

在 `initialize()` 方法中添加：

```python
# Create config_snapshots table
await self._db.execute("""
    CREATE TABLE IF NOT EXISTS config_snapshots (
        id            INTEGER PRIMARY KEY AUTOINCREMENT,
        version       TEXT NOT NULL,
        config_json   TEXT NOT NULL,
        description   TEXT DEFAULT '',
        created_at    TEXT NOT NULL,
        created_by    TEXT DEFAULT 'user',
        is_active     INTEGER DEFAULT 0
    )
""")

# Create index for version lookup
await self._db.execute("""
    CREATE INDEX IF NOT EXISTS idx_snapshots_version ON config_snapshots(version)
""")

# Create index for active snapshot
await self._db.execute("""
    CREATE INDEX IF NOT EXISTS idx_snapshots_active ON config_snapshots(is_active)
""")
```

---

### 步骤 3: 实现仓储方法

**文件**: `src/infrastructure/signal_repository.py`

```python
async def create_config_snapshot(
    self,
    version: str,
    config_json: str,
    description: str = "",
    created_by: str = "user",
) -> int:
    """Create a new config snapshot."""
    created_at = datetime.now(timezone.utc).isoformat()

    # Deactivate all existing snapshots
    await self._db.execute(
        "UPDATE config_snapshots SET is_active = 0 WHERE is_active = 1"
    )

    cursor = await self._db.execute(
        """
        INSERT INTO config_snapshots (version, config_json, description, created_at, created_by, is_active)
        VALUES (?, ?, ?, ?, ?, 1)
        """,
        (version, config_json, description, created_at, created_by),
    )
    await self._db.commit()
    return cursor.lastrowid

async def get_config_snapshots(
    self,
    limit: int = 50,
    offset: int = 0,
) -> dict:
    """Get all config snapshots with pagination."""
    # Get total count
    async with self._db.execute(
        "SELECT COUNT(*) as count FROM config_snapshots"
    ) as cursor:
        row = await cursor.fetchone()
        total = row["count"]

    # Get paginated data
    async with self._db.execute(
        """
        SELECT * FROM config_snapshots
        ORDER BY created_at DESC
        LIMIT ? OFFSET ?
        """,
        (limit, offset),
    ) as cursor:
        rows = await cursor.fetchall()
        return {"total": total, "data": [dict(row) for row in rows]}

async def get_config_snapshot_by_id(self, snapshot_id: int) -> Optional[dict]:
    """Get a single snapshot by ID."""
    async with self._db.execute(
        "SELECT * FROM config_snapshots WHERE id = ?", (snapshot_id,)
    ) as cursor:
        row = await cursor.fetchone()
        return dict(row) if row else None

async def get_active_config_snapshot(self) -> Optional[dict]:
    """Get the currently active snapshot."""
    async with self._db.execute(
        "SELECT * FROM config_snapshots WHERE is_active = 1 ORDER BY created_at DESC LIMIT 1"
    ) as cursor:
        row = await cursor.fetchone()
        return dict(row) if row else None

async def activate_config_snapshot(self, snapshot_id: int) -> bool:
    """Activate a snapshot (deactivate all others first)."""
    # Deactivate all
    await self._db.execute("UPDATE config_snapshots SET is_active = 0")

    # Activate target
    cursor = await self._db.execute(
        "UPDATE config_snapshots SET is_active = 1 WHERE id = ?", (snapshot_id,)
    )
    await self._db.commit()
    return cursor.rowcount > 0

async def delete_config_snapshot(self, snapshot_id: int) -> bool:
    """Delete a snapshot."""
    cursor = await self._db.execute(
        "DELETE FROM config_snapshots WHERE id = ?", (snapshot_id,)
    )
    await self._db.commit()
    return cursor.rowcount > 0
```

**验收标准**:
- [ ] 所有方法可正常工作
- [ ] 单元测试通过率 100%

---

### 步骤 4: 实现 API 端点

**文件**: `src/interfaces/api.py`

```python
# Request/Response models
class ConfigSnapshotCreate(BaseModel):
    version: str
    config_json: str
    description: str = ""
    created_by: str = "user"

class ConfigSnapshotResponse(BaseModel):
    id: int
    version: str
    config_json: str
    description: str
    created_at: str
    created_by: str
    is_active: bool

class ConfigSnapshotListResponse(BaseModel):
    total: int
    data: List[ConfigSnapshotResponse]

# API endpoints
@app.post("/api/config/snapshots", response_model=ConfigSnapshotResponse)
async def create_snapshot(request: ConfigSnapshotCreate):
    """Create a new config snapshot."""
    snapshot_id = await repository.create_config_snapshot(
        version=request.version,
        config_json=request.config_json,
        description=request.description,
        created_by=request.created_by,
    )
    return ConfigSnapshotResponse(
        id=snapshot_id,
        version=request.version,
        config_json=request.config_json,
        description=request.description,
        created_at=datetime.now(timezone.utc).isoformat(),
        created_by=request.created_by,
        is_active=True,
    )

@app.get("/api/config/snapshots", response_model=ConfigSnapshotListResponse)
async def list_snapshots(limit: int = 50, offset: int = 0):
    """List all config snapshots."""
    result = await repository.get_config_snapshots(limit=limit, offset=offset)
    return ConfigSnapshotListResponse(
        total=result["total"],
        data=[ConfigSnapshotResponse(**item) for item in result["data"]],
    )

@app.get("/api/config/snapshots/{snapshot_id}", response_model=ConfigSnapshotResponse)
async def get_snapshot(snapshot_id: int):
    """Get a single snapshot by ID."""
    snapshot = await repository.get_config_snapshot_by_id(snapshot_id)
    if not snapshot:
        raise HTTPException(status_code=404, detail="Snapshot not found")
    return ConfigSnapshotResponse(**snapshot)

@app.post("/api/config/snapshots/{snapshot_id}/activate")
async def activate_snapshot(snapshot_id: int):
    """Activate a config snapshot (rollback to this version)."""
    snapshot = await repository.get_config_snapshot_by_id(snapshot_id)
    if not snapshot:
        raise HTTPException(status_code=404, detail="Snapshot not found")

    success = await repository.activate_config_snapshot(snapshot_id)
    if success:
        # TODO: Trigger config reload with snapshot config
        return {"message": f"Activated snapshot {snapshot_id}"}
    else:
        raise HTTPException(status_code=500, detail="Failed to activate snapshot")

@app.delete("/api/config/snapshots/{snapshot_id}")
async def delete_snapshot(snapshot_id: int):
    """Delete a config snapshot."""
    success = await repository.delete_config_snapshot(snapshot_id)
    if not success:
        raise HTTPException(status_code=404, detail="Snapshot not found")
    return {"message": f"Deleted snapshot {snapshot_id}"}
```

**验收标准**:
- [ ] 所有端点可正常访问
- [ ] 使用 `pytest` 测试通过

---

### 步骤 5: 编写单元测试

**文件**: `tests/unit/test_config_snapshot.py`

```python
import pytest
import json
from decimal import Decimal
from src.domain.models import ConfigSnapshot

class TestConfigSnapshot:
    """Test ConfigSnapshot model."""

    def test_create_snapshot(self):
        snapshot = ConfigSnapshot(
            version="v1.0.0",
            config_json='{"risk": {"max_loss_percent": 0.01}}',
            description="Initial snapshot",
            created_by="test_user",
        )
        assert snapshot.version == "v1.0.0"
        assert snapshot.is_active is False

    def test_json_serialization(self):
        snapshot = ConfigSnapshot(
            version="v1.0.0",
            config_json=json.dumps({"test": "data"}),
        )
        data = snapshot.model_dump()
        assert "version" in data
        assert "config_json" in data
```

**验收标准**:
- [ ] 所有测试通过
- [ ] 覆盖核心逻辑

---

## 验收标准

- [ ] ConfigSnapshot 模型可正常实例化
- [ ] 数据库表创建成功
- [ ] 所有仓储方法可正常工作
- [ ] API 端点可通过 Postman/curl 访问
- [ ] 单元测试通过率 100%
- [ ] Git 提交完成

---

## 开始工作

在窗口 1 中运行：
```bash
# 确认在项目根目录
pwd

# 开始第一步
```

完成每一步后，向 Claude 报告进度。

---

*任务文档创建时间：2026-03-27*
